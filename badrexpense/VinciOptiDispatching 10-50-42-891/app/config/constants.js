// Define your WebSocket URL here (replace with your actual server address)
export const WS_URL = 'ws://10.171.38.27:8000/ws/technician/';

// Add any other constants your app needs
export const API_URL = 'http://10.171.38.27:8000/api/';

// Default settings
export const DEFAULT_LOCATION = {
  latitude: 0,
  longitude: 0,
};

// Add more constants as needed. ifconfig | grep "inet " | grep -v "127.0.0.1"
